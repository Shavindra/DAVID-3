'use strict';

/**
 * @ngdoc service
 * @name david3App.d3Service
 * @description
 * # d3Service
 * Factory in the david3App.
 */
d3ngApp.factory('d3Service', ['$document', '$window', '$q', '$rootScope',
        function ($document, $window, $q, $rootScope) {
           
        }
]);